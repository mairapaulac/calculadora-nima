PORT=3333
CORS_ORIGIN=http://localhost:5173
# Caminho do arquivo SQLite. Em producao (Render), aponte para um disco persistente,
# ex: /var/data/budgets.db (veja README - secao de deploy).
DATABASE_PATH=./data/budgets.db
